name: build
description: Check if the package can be built without errors.

runs:
  using: "composite"
  steps:
    - name: Build Package
      run: |
        python setup.py build --verbose --dry-run
      shell: bash
